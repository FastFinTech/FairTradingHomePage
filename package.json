{
  "name": "fairtrading-homepage",
  "version": "1.0.0",
  "description": "FairTrading homepage with live reload",
  "scripts": {
    "preview": "live-server --port=8080 --open=/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}

